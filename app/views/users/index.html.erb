 
<% provide(:title, 'Listing') %>
 <h1>Listing </h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
</table>

<%= will_paginate %>

  <ul class="users">
  <%= render @users %>
  </ul>

<%= will_paginate %>
 
