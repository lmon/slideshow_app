<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
                                           
   <%= stylesheet_link_tag "colorbox.css", media: "all",
                                           "data-turbolinks-track" => true %>
    
    <%= csrf_meta_tags %>

    <!-- JS needs to be here , per https://coderwall.com/p/ypzfdw/faster-page-loads-with-turbolinks -->
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

    <%= javascript_include_tag "jquery.colorbox.js", "data-turbolinks-track" => true %>


    <%= render 'layouts/shim' %>
  </head>

<% if (current_user && current_user.admin?)  %>
  <body class="adminmode"> 
<% else %>
  <body> 
<% end %>

  <%= render 'layouts/header' %>
   
    <div class="container-fluid">
    <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

        <%= yield %>

  <%= render 'layouts/footer' %>
  
  <%= debug(params) if Rails.env.development? %>

</div>



<script type="text/javascript">
  document.getElementById("demo").innerHTML = $(document).width();;
</script>

<%= javascript_include_tag "galleries.js", "galleries", "data-turbolinks-track" => false %>

  </body>
</html>